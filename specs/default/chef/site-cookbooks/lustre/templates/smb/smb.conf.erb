[global]
        workgroup = WORKGROUP
        server string = Samba Server %v
        security = user
        map to guest = bad user
        dns proxy = no

[lustre]
        path = /lustre
        browsable = yes
        writable = yes
        guest ok = yes
        read only = no
        force user = <%= node['cyclecloud']['cluster']['user']['name'] %>
